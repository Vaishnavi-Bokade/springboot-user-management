# springboot-user-management
This project is a RESTful CRUD application developed using Spring Boot. It provides APIs to perform Create, Read, Update, and Delete operations on database records. The application follows a layered architecture using Controller, Service, and Repository layers.
